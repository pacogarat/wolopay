shopApp.directive('calculatePrice',
    function($rootScope) {
        return {
            restrict: 'A',
            link : function(scope, element, attrs) {

                scope.article

            }
        };
    });