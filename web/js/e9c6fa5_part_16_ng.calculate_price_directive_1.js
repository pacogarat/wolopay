angular.module('shopApp').directive('calculatePrice',
    [function() {
        return {
            restrict: 'A',
            link : function(scope, element, attrs) {

                scope.article

            }
        };
    }]);